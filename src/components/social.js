import * as React from "react";
import { Container } from "theme-ui";

const Social = () => {
  return (
    <Container
      sx={{
        mb: "4",
        mt: "4",
      }}
    >
    </Container>
  );
};

export default Social;
